# Tracera data-source notebooks — Python dependencies
# Install:  python -m pip install -r requirements.txt

# Core
requests
pandas
python-dotenv

# Geospatial (SSURGO polygons, field boundaries)
geopandas
shapely
pyproj

# Weather / climate grids — gridMET via OPeNDAP (pydap is pure-Python; works on ARM64)
xarray
pydap

# Remote sensing — Sentinel-2 / Landsat NDVI via Microsoft Planetary Computer
pystac-client
planetary-computer
odc-stac
rasterio

# Markets / economics
yfinance
fredapi

# Notebooks / visualization
jupyter
matplotlib
nbformat
